let multiplo = (num1,num2) => { 
    if (num1 % num2 === 0)
   return "os números são multiplos";
   else return ("os números NÃO são multiplos");}
   
   console.log (multiplo(20,2));
   console.log (multiplo(15,7));